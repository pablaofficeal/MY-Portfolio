# 🔥 ПАВЛО | СИСТЕМНЫЙ И БЭКЕНД ИНЖЕНЕР

> "Мои системы не падают. Если система падает — виноват архитектор."

<div align="center">
  
![Terminal Portfolio](https://img.shields.io/badge/Тема-Мr_Robot_Терминал-red?style=flat-square)
![React](https://img.shields.io/badge/React-19.2.3-blue?style=flat-square)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8.2-blue?style=flat-square)
![Go](https://img.shields.io/badge/Backend-Go-00ADD8?style=flat-square)
![Docker](https://img.shields.io/badge/Deployment-Docker-2496ED?style=flat-square)

</div>

## 🚀 Что это за зверь?

Это не просто портфолио — это **иммерсивный терминальный опыт** вдохновлённый Mr. Robot и киберпанковской эстетикой. Представь себе, что ты хакер, который только что взломал систему, и перед тобой открывается личный досье на системного архитектора.

**Ключевые фишки:**
- 🖥️ **Анимированная загрузка терминала** с ASCII-артом Arch Linux
- 💻 **Интерактивный терминал** с typing-эффектом
- 🎨 **Тёмная киберпанковская тема** в стиле Fsociety
- ⚡ **Быстрая загрузка** и плавные анимации
- 🔄 **Full-stack архитектура** с React + Go

## 🎯 Особенности

### Frontend (React + TypeScript)
- **Vite** для молниеносной сборки
- **Анимированный терминал** с typing-эффектом
- **Адаптивный дизайн** для всех устройств
- **Тёмная тема** в стиле Mr. Robot
- **Интерактивные компоненты** с hover-эффектами

### Backend (Go)
- **REST API** на Go для обработки форм
- **PostgreSQL** для хранения данных
- **Docker** контейнеризация
- **Микросервисная архитектура**

## 🛠️ Технологический стек

### Frontend
```
React 19.2.3     — Современный UI фреймворк
TypeScript 5.8.2 — Типобезопасность
Vite 6.2.0       — Быстрая сборка
Lucide React     — Иконки
```

### Backend
```
Go               — Высокопроизводительный бэкенд
PostgreSQL       — Надёжная БД
Docker           — Контейнеризация
```

### Системные навыки
```
Python/Flask     — Бэкенд разработка
Go               — Системное программирование
C/C++/Rust       — Низкоуровневая разработка
Docker/Linux     — Инфраструктура
Security         — Информационная безопасность
```

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируй репозиторий:**
```bash
git clone https://github.com/yourusername/pavlo-systems-portfolio.git
cd pavlo-systems-portfolio
```

2. **Установи зависимости:**
```bash
npm install
```

3. **Запусти фронтенд:**
```bash
npm run dev
```

4. **Запусти бэкенд (в отдельном терминале):**
```bash
cd backend
go mod download
go run main.go
```

### Docker (Продакшн)

**Одной командой:**
```bash
docker-compose up -d
```

**Готово!** 🎉
- Frontend: http://localhost:3245
- Backend API: http://localhost:7654
- PostgreSQL: localhost:9876

## 📁 Архитектура проекта

```
pavlo-systems-portfolio/
├── components/          # React компоненты
│   ├── TerminalHero.tsx     # Главный терминал с загрузкой
│   ├── ManifestoTerminal.tsx # Философия через терминал
│   ├── SkillMatrix.tsx      # Матрица навыков
│   ├── ProjectLog.tsx       # Лог проектов
│   └── Navbar.tsx          # Навигация
├── backend/            # Go бэкенд
│   ├── main.go         # Основной сервер
│   ├── Dockerfile      # Контейнеризация
│   └── go.mod         # Go зависимости
├── types.ts           # TypeScript типы
├── docker-compose.yml # Docker оркестрация
└── vite.config.ts    # Конфигурация Vite
```

## 🎨 Компоненты

### TerminalHero
Анимированная загрузка системы с ASCII-артом Arch Linux. Создаёт эффект настоящего терминала с typing-эффектом.

### ManifestoTerminal
Интерактивный терминал с философией разработки. Показывает принципы работы через команды.

### SkillMatrix
Визуализация навыков в виде файловой системы Unix. Каждая категория — это директория с соответствующими навыками.

### ProjectLog
Лог проектов в стиле `ls -la` с правами доступа и тегами.

## 🔄 API Эндпоинты

### POST /api/feedback
Принимает обратную связь от посетителей.

**Тело запроса:**
```json
{
  "name": "Имя",
  "email": "email@example.com",
  "message": "Сообщение"
}
```

## 🎭 Тема и дизайн

Цветовая палитра вдохновлена Mr. Robot:
- **Чёрный** (#0a0a0a) — основной фон
- **Красный** (#ff0040) — акценты и важные элементы
- **Зелёный** (#00ff40) — успех и терминал
- **Белый** (#ffffff) — основной текст
- **Серый** (#666666) — второстепенный текст

## 🚀 Деплой

### Docker (Рекомендуется)
```bash
docker-compose up -d
```

### Ручной деплой
1. Собери фронтенд: `npm run build`
2. Запусти бэкенд: `cd backend && go run main.go`
3. Настрой nginx для фронтенда
4. Настрой reverse proxy для API

## 🤝 Контрибьютинг

Хочешь помочь? Отлично! Вот как:
1. Форкни репозиторий
2. Создай ветку (`git checkout -b feature/amazing-feature`)
3. Закоммить изменения (`git commit -m 'Add amazing feature'`)
4. Запушь ветку (`git push origin feature/amazing-feature`)
5. Открой Pull Request

----------------------------------------------------------------------------

<div align="center">
  <strong>🔥 Сделано с любовью к чистому коду и киберпанку 🔥</strong>
  <br>
  <em>"Это не баг — это фича безопасности"</em>
</div>